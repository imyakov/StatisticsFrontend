<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dimension Statistics</title>
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
    }
    th, td {
      border: 1px solid #ddd;
      padding: 8px;
      text-align: left;
    }
    th {
      background-color: #f2f2f2;
    }
  </style>
</head>
<body>
  <h1>Dimension Statistics</h1>
  <table>
    <thead>
      <tr>
        <th>Store</th>
        <th>Slot</th>
        <th>Total Orders</th>
        <th>Wrong Slot Proposals</th>
      </tr>
    </thead>
    <tbody>
      <% data.forEach(item => { %>
      <tr>
        <td><%= item.store %></td>
        <td><%= item.slot %></td>
        <td><%= item.total_orders %></td>
        <td><%= item.wrong_slot_proposals %></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
</body>
</html>
